# ===================================
# PRODUCTION CONFIGURATION
# ===================================

spring:
  # ===================================
  # DATABASE - SUPABASE (Production)
  # ===================================
  datasource:
    url: ${DB_URL:jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:6543/postgres}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:Zb6umRu9f8EwnsnU}
    hikari:
      maximum-pool-size: ${DB_POOL_SIZE:10}
      minimum-idle: ${DB_MIN_IDLE:5}
      connection-timeout: 30000

  jpa:
    show-sql: false

  # ===================================
  # REDIS - PRODUCTION
  # ===================================
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      timeout: 5000

  cache:
    redis:
      time-to-live: ${CACHE_TTL:3600000}

  # ===================================
  # EMAIL - PRODUCTION
  # ===================================
  mail:
    username: ${MAIL_USERNAME:namphuongtest@gmail.com}
    password: ${MAIL_PASSWORD:njzd jrec qvau wvkh}

# ===================================
# CLOUDINARY - PRODUCTION
# ===================================
cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:ddpbhytdh}
  api-key: ${CLOUDINARY_API_KEY:142387517415863}
  api-secret: ${CLOUDINARY_API_SECRET:FPFMohinoScDgNqSHCHa46Fs_c8}

# ===================================
# JWT SECRET - PRODUCTION
# ===================================
jwt:
  base64-secret: ${JWT_SECRET:VG9pVGVuTGFQaHVvbmdEYW5nSG9jU3ByaW5nU2VjdXJpdHlWYVRvaUNhbk1vdENhaUtleVJhdExhRGFpMTIzNDU2Nzg5}

# ===================================
# GOOGLE - PRODUCTION
# ===================================
google:
  client-id: ${GOOGLE_CLIENT_ID:959314408688-jvu23ua7f4ljpherafbf0mjm4m3l1spj.apps.googleusercontent.com}

# ===================================
# APP CONFIG - PRODUCTION
# ===================================
app:
  frontend-url: ${FRONTEND_URL:http://localhost:3000}

# ===================================
# LOGGING - PRODUCTION (Less verbose)
# ===================================
logging:
  level:
    org.springframework: ERROR
    com.movieapp: ERROR
    org.hibernate.SQL: ERROR